# Problem:

# Create a custom module called greetings.py with a function say_hello(name) that prints "Hello, [name]!"
# In a separate script, import the greetings module and call say_hello("Alice").

import greetings

greetings.say_hello("ALice")